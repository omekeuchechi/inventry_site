const navSlideBtn = document.getElementById('admin-slider-btn');
const navSlide = document.querySelector('.show');

navSlideBtn.addEventListener('click', () => {
    // navSlide.classList.toggle('admin-nav-active');
    // navSlideBtn.classList.toggle('fa-angle-right');
    // style.backGroundColor = 'red';
});